{
  "mcpServers": {
    "boostedtravel": {
      "command": "npx",
      "args": ["-y", "boostedtravel-mcp"],
      "env": {
        "BOOSTEDTRAVEL_API_KEY": "YOUR_API_KEY_HERE (optional — omit for local-only search with 75 airline connectors)"
      }
    }
  }
}
