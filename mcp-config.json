{
  "mcpServers": {
    "letsfg": {
      "command": "npx",
      "args": ["-y", "letsfg-mcp"],
      "env": {
        "LETSFG_API_KEY": "YOUR_API_KEY_HERE (optional — omit for local-only search with 75 airline connectors)"
      }
    }
  }
}
